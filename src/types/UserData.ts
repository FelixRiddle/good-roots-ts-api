/**
 * User object
 * 
 * It's wrong to have so many '?'
 */
interface UserData {
    confirmPassword?: string;
    email?: string;
    name?: string;
    password?: string;
};

export default UserData;
